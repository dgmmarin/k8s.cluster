# No-op resource so the secrets Application never has an empty source
# directory (ArgoCD errors on directory sources with zero manifests).
# Safe to delete once a real SealedSecret lives here.
apiVersion: v1
kind: ConfigMap
metadata:
  name: secrets-placeholder
  namespace: argocd
data: {}
