---
# Generated by playbooks/01-provision.yml (Hetzner) or
# playbooks/01-provision-netcup.yml (bring-your-own VPS) — do not edit by hand.
all:
  children:
    k3s:
      hosts:
        k3s-node:
          ansible_host: {{ server_ip }}
          ansible_user: root
{% if host_firewall | default(false) %}
          # Host firewall (ufw) — bring-your-own VPS only (see base role).
          host_firewall: true
          admin_ip: {{ effective_admin_ip }}
{% endif %}
